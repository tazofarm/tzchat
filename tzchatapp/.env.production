# ============================
# 📦 프로덕션 환경(.env.production)
# - 앱(Production) 빌드용
# - 백엔드와 통신은 반드시 HTTPS (쿠키 SameSite=None; Secure 조건 충족)
# - 이 값은 axios baseURL로 사용되며, 라우트 경로는 코드에서 `/api/...`를 붙입니다.
# - ✅ 끝에 슬래시(/)를 붙이지 마세요.
# - 세션 쿠키 대신 JWT 토큰 방식을 사용합니다.
# ============================

# 프로덕션 백엔드(Express) 주소
VITE_API_BASE_URL=https://tzchat.duckdns.org

# 인증 모드 (JWT 고정)
VITE_AUTH_MODE=jwt
