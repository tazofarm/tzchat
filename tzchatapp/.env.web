# ============================
# 🌐 웹 빌드/배포 환경(.env.web)
# - 웹 브라우저용 빌드/배포 시 사용
# - 백엔드와 통신은 HTTPS 필수
# - axios baseURL = VITE_API_BASE_URL, 경로는 코드에서 `/me`, `/login` 등 상대경로만 씁니다.
# - ✅ 끝에 슬래시(/)를 붙이지 마세요.
# ============================

# 웹 배포 백엔드(Express) 주소 (+ /api 포함)
VITE_API_BASE_URL=https://tzchat.duckdns.org/api

# 인증 모드 (JWT 고정)
VITE_AUTH_MODE=jwt
