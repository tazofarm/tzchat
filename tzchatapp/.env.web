# ============================
# 🌐 웹 빌드/배포 환경(.env.web)
# - 웹 브라우저용 빌드/배포 시 사용
# - 백엔드와 통신은 HTTPS 필수
# - axios baseURL은 여기 정의된 값을 사용하며, 코드에서는 `/api/...` 경로를 붙입니다.
# - ✅ 끝에 슬래시(/)를 붙이지 마세요.
# ============================

# 웹 배포 백엔드(Express) 주소
VITE_API_BASE_URL=https://tzchat.duckdns.org

# 인증 모드 (JWT 고정)
VITE_AUTH_MODE=jwt
