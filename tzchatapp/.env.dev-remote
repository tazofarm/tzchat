# ============================
# 🌐 원격-개발 환경(.env.dev-remote)
# - 프론트(dev:remote, 포트 8081) → 원격 서버(https://tzchat.duckdns.org) 직접 호출
# - 이 값은 axios baseURL로 사용되며, 라우트 경로는 코드에서 `/api/...`를 붙입니다.
# - ✅ 끝에 슬래시(/)를 붙이지 마세요.
# - 세션 쿠키 대신 JWT 토큰 방식을 사용해야 합니다.
# ============================

# 원격 백엔드(Express) 주소
VITE_API_BASE_URL=https://tzchat.duckdns.org

# 인증 모드 (세션 대신 JWT)
VITE_AUTH_MODE=jwt
