# ============================
# 🌐 원격-개발 환경(.env.dev-remote)
# - 프론트(dev:remote, 포트 8081) → 원격 서버(https://tzchat.duckdns.org/api)
# - axios baseURL = VITE_API_BASE_URL
# - 끝에 슬래시(/) 붙이지 않음
# ============================

# Axios용 원격 백엔드(Express) 주소 (+ /api 포함)
VITE_API_BASE_URL=https://tzchat.duckdns.org/api

# Socket.IO 오리진(프로토콜+호스트만) → socket.ts가 최우선 사용
VITE_WS_BASE=https://tzchat.duckdns.org

# 인증 모드
VITE_AUTH_MODE=jwt

# 디버그용 모드 플래그 (api.ts가 함께 참고)
VITE_MODE=dev-remote
