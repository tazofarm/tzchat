# ============================
# 🌱 개발 환경(.env.development)
# - Vite는 VITE_ 접두어만 클라이언트에 노출됩니다.
# - axios baseURL = VITE_API_BASE_URL, 경로는 코드에서 `/me`, `/login` 등 상대경로만 씁니다.
# - ✅ 끝에 슬래시(/)를 붙이지 마세요. (예: http://localhost:2000 ✅, http://localhost:2000/ ❌)
# - dev 모드에서는 Vite 프록시로 /api → http://localhost:2000 연결도 가능합니다.
# ============================

# 로컬 백엔드(Express) 주소
VITE_API_BASE_URL=http://localhost:2000/api


# VITE_API_BASE_URL=https://tzchat.duckdns.org/api
# (선택) 로컬 네트워크 테스트 예시 — 필요 시만 사용하고 주석 유지
# VITE_API_BASE_URL=http://192.168.0.7:2000/api

# 인증 모드 (JWT 고정)
VITE_AUTH_MODE=jwt
