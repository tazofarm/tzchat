# ============================
# 🌱 개발 환경(.env.development)
# - Vite는 VITE_ 접두어만 클라이언트에 노출됩니다.
# - 이 값은 axios baseURL로 사용되며, 라우트 경로는 코드에서 `/api/...`를 붙입니다.
# - ✅ 끝에 슬래시(/)를 붙이지 마세요. (예: http://localhost:2000 ✅, http://localhost:2000/ ❌)
# - dev 모드에서는 Vite 프록시로도 /api → http://localhost:2000 으로 연결될 수 있습니다.
# ============================

# 로컬 백엔드(Express) 주소
VITE_API_BASE_URL=http://localhost:2000

# (선택) 로컬 네트워크 테스트 예시 — 필요 시만 사용하고 주석 유지
# VITE_API_BASE_URL=http://192.168.0.7:2000

# (참고) 배포/원격 환경에서는 dev-remote/production 전용 파일을 사용하세요.
# 여기서는 주석으로만 남겨 둡니다.
# VITE_API_BASE_URL=https://tzchat.duckdns.org

# 인증 모드 (JWT 고정)
VITE_AUTH_MODE=jwt
