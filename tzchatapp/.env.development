# ============================
# 🌱 개발 환경 (.env.development)
# - Vite는 VITE_ 접두어만 클라이언트에 노출됩니다.
# - axios baseURL = VITE_API_BASE_URL (상대 경로로 `/me`, `/login` 등만 사용)
# - ✅ 끝에 슬래시(/) 붙이지 마세요. (예: http://localhost:2000 ✅, http://localhost:2000/ ❌)
# - 모든 모드에서 목적지는 .env 값으로만 결정합니다. (Vite 프록시 미사용)
# ============================

# REST API (로컬 백엔드)
VITE_API_BASE_URL=http://localhost:2000/api

# Socket.IO 오리진 (프로토콜+호스트만)
VITE_WS_BASE=http://localhost:2000

# 인증 모드
VITE_AUTH_MODE=jwt

# (옵션) 로컬 네트워크 테스트 예시 — 필요 시만 사용하고 주석 유지
# VITE_API_BASE_URL=http://192.168.0.7:2000/api
# VITE_WS_BASE=http://192.168.0.7:2000
